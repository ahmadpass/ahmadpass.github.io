window.addEventListener('DOMContentLoaded', () => { loadTemplateLayout() }); async function loadTemplateLayout() { var index = $('#index').text(); if (index == "0") { await $("#navigation").load("shared/navigation.html"); await $("#footer").load("shared/footer.html"); await $("#postPreview").load("shared/post-preview.html") } else { await $("#navigation").load("../shared/navigation.html"); await $("#footer").load("../shared/footer.html"); await $("#postPreview").load("../shared/post-preview.html") }; var currTitle = document.title; var baseTitle = "Code-Bite | "; $(document).attr("title", baseTitle + currTitle); var intermezzo = '------------------\n' + '< Happy Reading! >\n' + '------------------\n' + '        ' + '\\   ^__^\n' + '         ' + '\\  (oo)\\_______\n' + '            ' + '(__)\\       )\\/\\\n' + '                ' + '||----w |\n' + '                ' + '||     ||'; console.log(intermezzo) }; async function loadNav() { let scrollPos = 0; const mainNav = $('#mainNav')[0]; const headerHeight = mainNav.clientHeight; window.addEventListener('scroll', function () { const currentTop = document.body.getBoundingClientRect().top * -1; if (currentTop < scrollPos) { if (currentTop > 0 && mainNav.classList.contains('is-fixed')) { mainNav.classList.add('is-visible') } else { mainNav.classList.remove('is-visible', 'is-fixed') } } else { mainNav.classList.remove(['is-visible']); if (currentTop > headerHeight && !mainNav.classList.contains('is-fixed')) { mainNav.classList.add('is-fixed') } } scrollPos = currentTop }) }; $(window).on('load', function () { setTimeout(() => { loadNav() }, 1) });